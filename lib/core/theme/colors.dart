import 'package:flutter/material.dart';

class AppColors {
  // Softer, calm green (steady but not heavy)
  static const Color primaryColor = Color(0xFF66BB6A); // soft green

  // Very light background (clean content reading)
  static const Color backgroundColor = Color(0xFFFFFFFF);

  // Subtle grey buttons (barely there)
  static const Color buttonBackground = Color(0xFFF5F5F5);

  // Text hierarchy
  static const Color textPrimary = Color(0xFF1F1F1F); // clean dark
  static const Color textSecondary = Color(0xFF7A7A7A); // soft muted grey

  // Accent — gentle, not screaming
  static const Color accent = Color(
    0xFFFFD966,
  ); // warm, soft highlight (pastel yellow)
}
